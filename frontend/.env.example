# Chrome extension ID from chrome://extensions (Developer mode → ID under extension name).
# Required for the React app to push Firebase auth directly to the extension background.
VITE_EXTENSION_ID=peihmoppempfobphbdchmkbgicmieljl
